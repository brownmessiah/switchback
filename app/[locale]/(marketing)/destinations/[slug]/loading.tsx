import { CollectionPageSkeleton } from '@/components/collection-page-skeleton'

/** Destination collection route skeleton (issue 25). */
export default function DestinationLoading() {
  return <CollectionPageSkeleton />
}
